#include <stdlib.h>
#include <string.h>
#include <iostream>

using namespace std;

int main(void) {
    int N;
    cin >> N;
    int i=0, A[N];
    int64_t a;
    while (cin >> a) {
        A[i] = a;
        i++;
    }       
}